# Path /etc/systemd/system/start-minecraft.service

[Unit]
Description=Start minecraft

[Service]
Type=simple
ExecStartPre=/bin/sleep 60
ExecStart=/usr/bin/docker-compose -f /raid/dockerized_server/docker-compose.yml up -d

[Install]
WantedBy=multi-user.target
